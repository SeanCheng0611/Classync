# Docker Compose 用的基礎設施層變數。
# 應用程式本身的環境變數（LINE Login、JWT_SECRET、DEPLOY_WEBHOOK_SECRET 等）
# 請參考 server/.env.example，複製成 server/.env，docker-compose.yml 會自動讀取。

# backend 對外開放的 port（同時也是 container 內部監聽的 port）
PORT=4000
